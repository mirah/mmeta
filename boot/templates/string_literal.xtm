begin  # '{{STRING}}'
  {{VAR}} = _pos
  raise @error if {{VAR}} + {{LENGTH}} > @chars.length{{#CHAR}}
  raise @error unless @chars[{{VAR}}] == {{CHAR}}
  {{VAR}} += 1{{/CHAR}}
  self._pos = {{VAR}}
  "{{STRING}}"
end